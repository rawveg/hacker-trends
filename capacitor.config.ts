import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.sparklingsalamandernap',
  appName: 'sparkling-salamander-nap',
  webDir: 'dist'
};

export default config;
